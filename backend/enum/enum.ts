export enum AssessmentStatus{
    "Pending",
    "Approved",
    "Cancelled",
    "Forwarded"
}

export enum position {
  FRONTEND = 'frontend',
  BACKEND = 'backend',
  TESTING = 'testing',
  HR = 'hr'
};

export enum role{
  EMPLOYEE = "employee",
  LEAD = "lead",
  HR = "hr",
}

export enum teamName{
  INFORIVER = "inforiver",
  INFOBRIDGE = "infobridge",
  VALQ = "valq",
}